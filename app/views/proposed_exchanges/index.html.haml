%h2.page-header= t(".header")

.table-responsive
  %table.exchanges.table.table-striped
    %thead
      %tr
        %th= t(".exchanges")
        %th
    %tbody
      = render partial: "exchange", collection: @exchanges
